package iitr.paras.melper;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        //make list view in this activity containing name and check box
        //add + button to add new locations
        //still need to add maps activity which will be added after anusha's work
        //this activity will also contain settings to stop the app.

    }
}
